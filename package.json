{
    "name": "brh-hands-on-project",
    "dependencies": {
        "@sap/cds": "^5.9.7",
        "express": "^4.18.1",
        "sqlite3": "^5.0.8"
    },
    "scripts": {
        "start": "cds run",
        "test": "jest",
        "coverage":"npm run test -- --coverage"
      },
    
    "cds": {
        "requires": {
            "db": {
                "kind": "sql"
            }
        }
    },
    "devDependencies": {
        "@types/jest": "^28.1.2",
        "jest": "^28.1.1"
    }
}
